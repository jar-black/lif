# ============================================================
# Life MCP Hub — Environment Configuration
# Copy this file to .env and fill in your values:
#   cp .env.example .env
# ============================================================

# ------------------------------------------------------------
# Domain
# Point your DNS A record to your home IP, then set this.
# Examples:
#   DOMAIN=home.example.com
#   DOMAIN=abc123.cfargotunnel.com   (Cloudflare Tunnel)
# ------------------------------------------------------------
DOMAIN=your-domain-or-tunnel.com

# ------------------------------------------------------------
# OAuth 2.0 — Single-user auth for Claude.ai connector
# OWNER_PASSWORD: the password you'll type in the login page
# JWT_SECRET: random 64-char string (keep secret!)
#   Generate: python3 -c "import secrets; print(secrets.token_hex(32))"
# ------------------------------------------------------------
OWNER_PASSWORD=change-me-strong-password
JWT_SECRET=change-me-64-char-random-hex-string-generate-with-python
JWT_EXPIRES_HOURS=1
REFRESH_EXPIRES_DAYS=30

# ------------------------------------------------------------
# Proton Mail Bridge
# PROTON_EMAIL: your Proton Mail address
# PROTON_BRIDGE_PASSWORD: the bridge app password (NOT your Proton account password)
#   Get it from: Proton Mail app → Settings → Proton Mail Bridge
#   (After running: docker compose exec proton-bridge proton-bridge --noninteractive login)
# ------------------------------------------------------------
PROTON_EMAIL=your@proton.me
PROTON_BRIDGE_PASSWORD=change-me-bridge-app-password

# ------------------------------------------------------------
# Grocery Store Locations
# Customize names to match your local stores.
# URLs should point to the deals/offers page.
# ------------------------------------------------------------
STORE_1_NAME=Hemköp Masthugget
STORE_1_URL=https://www.hemkop.se/erbjudanden

STORE_2_NAME=Willys Första Långgatan
STORE_2_URL=https://www.willys.se/erbjudanden

STORE_3_NAME=Lidl Kungsgatan 16
STORE_3_URL=https://www.lidl.se/erbjudanden-och-rabatter
